Drop your audio files here (Vite serves `/public` at the site root).

Required filenames:
- `tick.mp3` (turn timer tick) — `tic.mp3` also supported as a fallback
- `music.mp3` (background music, loops during matches at 60% volume)
- `win.mp3` (win celebration)
